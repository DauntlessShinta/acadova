PORT=5000
MONGO_URI=your_mongodb_atlas_connection_string
JWT_SECRET=your_long_random_jwt_secret
# Optional when the frontend calls the API directly from another origin.
FRONTEND_ORIGIN=http://localhost:5173
